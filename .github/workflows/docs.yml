name: Docs

on:
  push:
    branches:
      - beta
      - master
    paths:
      - docs/**
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      fr_fr: ${{ steps.filter.outputs.fr_fr }}
      translated: ${{ steps.filter.outputs.translated }}
      images: ${{ steps.filter.outputs.images }}
      manual: ${{ github.event_name == 'workflow_dispatch' }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          base: ${{ github.ref_name }}
          filters: |
            fr_fr:
              - 'docs/fr_FR/**'
            translated:
              - 'docs/en_US/**'
              - 'docs/es_ES/**'
              - 'docs/de_DE/**'
            images:
              - 'docs/images/**'

  translate:
    needs: changes
    if: needs.changes.outputs.fr_fr == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: jeedom/jeetranslate-docs@main
        with:
          deepl_api_key: ${{ secrets.DEEPL_API_KEY }}
          target_languages: en_US,es_ES,de_DE

  publish:
    needs: changes
    if: needs.changes.outputs.translated == 'true' || needs.changes.outputs.images == 'true' || needs.changes.outputs.manual == 'true'
    uses: jeedom/workflows/.github/workflows/docs-publish.yml@main
    secrets:
      docs_repo_token: ${{ secrets.DOCS_REPO_TOKEN }}
